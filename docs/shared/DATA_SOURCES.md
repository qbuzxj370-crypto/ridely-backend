# Ridely 활용 데이터 목록

> 공모전 제출물의 활용 데이터 명세 + 출처표시 근거 + 팀 내 소스 파악용.
> 새 데이터를 붙이면 이 문서에 먼저 추가한다.

---

## 1. 한눈에 보기

| # | 데이터 | 제공기관 | 방식 | 적재 대상 | 상태 |
|---|---|---|---|---|---|
| 1 | 국문 관광정보 서비스_GW (TourAPI) | 한국관광공사 | API | `tour_attraction` | ✅ 적재 완료 (710) |
| 2 | 자전거길 DB | 행정안전부 | **파일** | `national_bike_route`, `route_facility` | ✅ 적재 완료 (13노선 / 1,024) |
| 3 | 서울시 자전거 편의시설 | 서울특별시 | API | `route_facility`(AIR_PUMP), `repair_shop` | ✅ 적재 완료 (1,100 / 24) |
| 4 | 서울시 공공자전거 대여소 정보 | 서울특별시 | API | `bike_station` | ✅ 적재 완료 (3,239) |
| 5 | 자전거 교통사고 다발지역 정보 | 도로교통공단 | API | `accident_zone` | ✅ 적재 완료 (111 / 2024년) |
| 6 | 자전거보관소정보 조회서비스 | 행정안전부 | API | `bike_parking` | 검증 완료, 적재 예정 |
| 7 | 서울시 공공자전거 실시간 대여정보 | 서울특별시 | API | (미적재, 실시간 조회) | 검증 완료 |
| 8 | 전국자전거도로표준데이터 | 행정안전부 | API | `bike_road` | **차후 반영** |
| 9 | 자전거대여수리점안내 | 한국수자원공사 | API/파일 | `repair_shop` | **보류** (MVP 구간 0건) |
| 10 | 공간정보 오픈플랫폼 WMS/WFS | 국토교통부 (V-World) | API | — | **미채택** (6번과 비교용) |
| 11 | 두루누비 정보 서비스 | 한국관광공사 | API | — | **미채택** — 자전거 코스 0건 (`SPIKE_DURUNUBI.md`, 결함 6.5) |
| 12 | 생활안전지도 자전거길 | 행정안전부 | WMS | — | **미채택** — 이미지만 제공, 좌표 추출 불가 |
| 13 | 자전거길(사고지점) | 행정안전부 | WMS | — | **미채택** — 12번과 동일 등록, 활용신청 불가 |

공공데이터가 아닌 외부 서비스는 4장에 따로 정리했다.

### 노선 형상 대체 소스 탐색 종료 (2026-08-11)

`national_bike_route`의 구간 경계를 담은 대체 소스를 찾아 네 곳을 확인했고, **전부 부적합**이다.
**행정안전부 자전거길 DB(2번) CSV + 좌표 간격 3km 분리로 확정한다** —
근거와 검증은 `SCHEMA_CHANGE_ROUTE_GEOM.md`.

| 소스 | 결과 |
|---|---|
| 두루누비 정보 서비스 (한국관광공사) | 코스 144건이 전부 걷기(DNWW). 자전거 0건 |
| 생활안전지도 자전거길 (`A2SM_BIKE`) | **WMS만 제공** = 렌더링된 이미지. 좌표를 얻을 수 없다 |
| 자전거길(사고지점) | 설명이 위 항목과 동일하고 사고지점 언급이 없다. 샘플 미표시, 활용신청 버튼 없음 |
| V-World WFS (서비스대상 169종) | 체육 카테고리에 **자전거보관소(점)만** 있고 노선 레이어가 없다. 교통의 `도로중심선`·정밀도로지도의 `보도구간`은 자전거길이 아니다 |

> ⚠️ 새 소스를 제안하기 전에 이 표를 먼저 볼 것. 같은 곳을 다시 파는 데 이미 시간을 썼다.
> 판별 기준 한 줄: **WMS는 이미지라 쓸 수 없고, 필요한 것은 WFS(벡터) 또는 파일(GeoJSON/SHP/GPX)이다.**
>
> 생활안전지도가 노선을 선(線)으로 표시한다는 것은 **행정안전부가 벡터 원본을 보유**한다는 뜻이다.
> 공공데이터포털 개선요청의 근거로 사용한다("없는 것을 만들어달라"가 아니라 "있는 것을 쓸 수 있는 형식으로").

---

## 2. 공공데이터 상세

### 2.1 국문 관광정보 서비스_GW (TourAPI)

| 항목 | 내용 |
|---|---|
| 제공기관 | 한국관광공사 |
| 식별번호 | `15101578` |
| 엔드포인트 | `https://apis.data.go.kr/B551011/KorService2` |
| 오퍼레이션 | `locationBasedList2` (위치 기반 조회) |
| 인증키 | `TOUR_API_KEY` (공공데이터포털) |
| 이용허락 | **이용허락범위 제한 없음** |

> 단, 제공 데이터 중 **사진 자료**는 피사체 명예훼손·인격권 침해 용도 및
> 기업 CI/BI 사용이 금지된다 (공공누리 1유형·3유형 이미지가 섞여 있음).
> 우리는 `first_image_url`·`thumbnail_url`을 화면 표시에만 쓴다.

**주의**

- 현행 서비스는 `KorService2`다. `KorService1`은 구버전이며 오퍼레이션명도 모두 `2`로 끝난다
- `mapX`=경도, `mapY`=위도 순서다
- 키 미등록·한도 초과 같은 포털 오류는 `_type=json`이어도 **XML로 응답**한다
- `areacode`·`sigungucode`·`cat1~3`은 **사용중지 항목이다.** 공고상 미표출 시행일이 2026-01-12인데 그 뒤로도 값이 계속 내려왔다. 시행일을 믿을 수 없으므로 **요청·응답 어느 쪽으로도 쓰지 않는다.** 지역은 `lDongRegnCd`, 분류는 `lclsSystm1~3`이 대체 항목이다 (6.4)

#### 신분류체계 (`lclsSystm1~3`) — 아직 쓰지 않지만 쓸 수 있다

「신분류체계정보 관광타입정보 연계 정의서」(260112_01) 기준. 소분류 240건이 관광타입 8종에 대응한다.

**요청 파라미터로는 분류체계와 관광타입을 함께 쓸 수 없다**(공지 230). 관광타입으로 받아 **응답의 `lclsSystm`으로 애플리케이션에서 거르는 것은 가능하다.**

우리가 적재하는 두 타입의 구성이다.

| 관광타입 | 소분류 | 대분류 |
|---|---|---|
| **12 관광지** | 105 | `EX` 체험관광 31 · `HS` 역사관광 24 · `NA` 자연관광 27 · `VE` 문화관광 23 |
| **14 문화시설** | 17 | `VE` 문화관광 17 (공연·전시·행사·교육시설) |

자전거 코스에 쓸 만한 중분류를 추려 두면 이렇다.

| 코드 | 이름 | 왜 |
|---|---|---|
| `NA02` | 자연경관(하천·해양) | 한강 구간 그 자체다 |
| `NA04` | 자연공원 | |
| `VE03` | 도시공원 | 실측에서 뽑힌 여의도공원·서울색공원이 여기다 |
| `HS01` | 역사유적지 | 실측의 망원정 터·절두산 순교성지 |
| `VE01` | 랜드마크관광 | 실측의 국회의사당 |

> **참고용이다. 저장 계획은 없다.** 지금은 `contentTypeId` 12·14로만 거르고 분류체계는 쓰지 않는다. 값은 응답에 오지만 `TourApiListResponse.Item`에 선언하지 않아 버려진다.
>
> 나중에 종류별로 갈라야 할 일이 생기면 선택지가 셋이다. "공원만" 수준은 `title` 매칭으로 대체로 잡힌다(한국어 시설명이 종류를 담는 편이다). 의미 기반 구분은 `overview`가 전 건에 있으니 계획된 임베딩(RAG)이 더 자연스럽다 — 분류체계는 격자가 고정돼 `VE03 도시공원`은 있어도 "강이 보이는 곳"은 없다. **분류 코드를 실제로 저장해야 하는 경우는 고정된 필터를 화면에 노출할 때 정도다.**
>
> 다만 `title` 매칭은 2.3에 적어 둔 원칙과 부딪친다 — 이름은 등록 당시 라벨이고, 이름으로 뒤집으면 우리 추측을 원본 입력 위에 두게 된다. `lclsSystm`은 제공기관이 매긴 값이라 그 점에서는 낫다.

---

### 2.2 자전거길 DB *(파일)*

| 항목 | 내용 |
|---|---|
| 제공기관 | 행정안전부 |
| 식별번호 | `3038533` |
| 형태 | ZIP (CSV 2 + XLSX 1) |
| 인증키 | 불필요 |
| 갱신 주기 | **수시 (1회성 데이터)** — 최종수정 2025-06-18 |
| 이용허락 | **이용허락범위 제한 없음** |

공식 설명이 **"국토종주 자전거길 13개"**로 명시한다. 코드북의 코드 14~46(강릉 경포호 산소길 등 지역 자전거길)은 부가 제공분이므로 `national_bike_route`는 **코드 1~13만 적재**한다.

**포함 파일**

| 파일 | 내용 | 규모 |
|---|---|---|
| `★국토종주 자전거길 노선좌표.csv` | 노선별 좌표 시퀀스 | 53,418행 / 노선 41개 |
| `★국토종주자전거길 주변시설 좌표정보.csv` | 인증센터·화장실·급수대·공기주입기 | 1,133행 |
| `★노선정보 코드북.xlsx` | 노선 코드 ↔ 노선명 매핑 | 41개 |

**검증 결과**

- 배포본 인코딩 **CP949**. 단 편집기에서 열어 저장하면 UTF-8로 바뀌므로
  **리더가 고정하지 않고 판별한다**(UTF-8 → MS949 순). 상세는 `BikeRouteCsvReader` 주석
- 시설 구분 4종 — 화장실 **688** / 급수대 185 / 인증센터 88 / **공기주입기 63** (중복 제거 후)
- 원본에 중복 109건. 적재 시 `(구분, 이름, 좌표)` 기준 제거 — 화장실 105 + 인증센터 4
- MVP 구간(한강 서울) 176건 — 화장실 97 / 급수대 75 / 인증센터 4. **공기주입기 0건**
  (그래서 AIR_PUMP는 서울시 API가 따로 필요하다 — 2.3 참조)
- 시설의 `이름` 컬럼이 **시설명이 아니라 노선명**이다 (급수대 75건이 전부 `"한강종주길"`)
- 노선명 표기가 코드북과 다르다 (`한강종주길` ↔ `한강종주자전거길`). 매핑 필요
- **두 CSV의 열 순서가 반대다.** 노선좌표는 3열=위도·4열=경도, 주변시설은 3열=경도·4열=위도
- 주변시설 CSV에는 **노선 코드가 없다.** FK는 좌표 기반(`ST_DWithin` 500m)으로 채운다

**적재 실측 (2026-08-12)**

| 대상 | 결과 |
|---|---|
| `national_bike_route` | 13노선 / 49,257점. 좌표 간격 3km 초과 시 파트 분리 → MultiLineString |
| `route_facility` | 1,133행 → **1,024건** (중복 109 제외) |
| 노선 연결 | **1,009건 (98.5%)**. 미연결 15건은 6.9.1 참조 |

> ⚠️ 원본에 결함 8건이 있다. **6.1에 정리했다** — 개선요청 현황과 함께 관리한다.

---

### 2.3 서울시 자전거 편의시설

| 항목 | 내용 |
|---|---|
| 제공기관 | 서울특별시 (디지털도시국 공간정보과) |
| 서비스명 | `tvBicycleEtc` |
| 엔드포인트 | `http://openapi.seoul.go.kr:8088/{KEY}/json/tvBicycleEtc/{시작}/{끝}/` |
| 인증키 | `SEOUL_OPEN_API_KEY` (서울 열린데이터광장) |
| 이용허락 | **공공누리 제1유형 — 출처표시 (상업적 이용·변경 가능)** ✅ |

**검증 결과 — 총 3,374건 (2026-08-12 적재 실측)**

- 좌표 결측 **0건**, `USE_YN` 전 건 "사용"
- **시설 종류를 구분하는 전용 필드가 없다.** `THMS_TYPE`·`THMS_NM`은 전 건 동일하다

#### 분류 규칙 (확정)

판정 로직은 `SeoulFacilityClassifier`에 있고 단위 테스트로 고정돼 있다.

| 판정 | 규칙 | 실측 | 적재 대상 |
|---|---|---|---|
| **AIR_PUMP** | 시설명에 **`주입기`** 포함 | **1,100** | `route_facility` |
| **REPAIR_SHOP** | `DTL_INFO_VL06`(운영형태) **또는** `VL07`(수리범위)이 채워짐 | **24** | `repair_shop` |
| OTHER | 그 외 (거치대·보관대) | 2,250 | **적재 안 함** |

**`주입기` 한 단어인 이유** — 변형이 전부 잡힌다. 오타(`자잔거`·`잦너거 공기주입기`),
공백 변형(`수동식 공기 주입기`), 방식 표기(태양광·자동식·기계식).
`공기주입`으로 잡으면 공백 변형을 놓쳐 1,098건이 된다.

**`VL08`을 쓰지 않는 이유** — 항목명은 "이동수리센터 순회일정"인데 값 7건 중 3건이
관리부서명이다. 08까지 넣으면 27건이 되고 `일반거치대` 2건이 수리센터로 잘못 분류된다.
상세는 6.7의 4번 참조.

**시설명으로 거르지 않는 이유** — `VL06`·`VL07`을 채웠다는 건 담당자가 "수리 기능이 있다"고
입력한 것이다. 시설명은 등록 당시 라벨일 뿐이고 거치대 옆 간이 수리대 사례도 있다.
이름으로 뒤집으면 우리 추측을 원본 입력 위에 두게 된다. 오탐 2건(6.9.2)을 감수한다.

**거치대·보관대를 적재하지 않는 이유** — `bike_parking` 소스는 행안부 자전거보관소 API로
확정돼 있다(2.6, 서울 2,698건). 함께 넣으면 중복 소스가 된다.

**주의**

- 1회 요청 최대 1,000건 (초과 시 `ERROR-336`)
- 인증키가 틀리면 json을 요청해도 **XML로 응답**한다
- https 미지원. `http` + 8088 포트 고정
- 1,000건 응답이 수 MB라 WebClient 기본 버퍼(256KB)로는 받을 수 없다
- **JSON 루트 키가 서비스명과 다를 수 있다** — 2.4·2.7 참조

---

### 2.4 서울시 공공자전거 대여소 정보

| 항목 | 내용 |
|---|---|
| 제공기관 | 서울특별시 |
| 서비스명 | `tbCycleStationInfo` (루트 키는 **`stationInfo`**) |
| 인증키 | `SEOUL_OPEN_API_KEY` |
| 이용허락 | **공공누리 제1유형 — 출처표시 (상업적 이용·변경 가능)** |

**적재 결과 — 3,239건** (2026-08-13). 스키마와 그대로 대응해 변경이 필요 없었다.
검증 시점(3,237건)보다 2건 늘었다. 대여소는 계속 신설·폐쇄되므로 `station_code` 기준 UPSERT +
`deactivate_stale`로 이번 실행에 안 들어온 대여소를 비활성 처리한다.

| 응답 | `bike_station` |
|---|---|
| `RENT_ID` (`ST-10`) | `station_code` UNIQUE |
| `RENT_NM` | `station_name` |
| `STA_LONG` / `STA_LAT` | `geom` |
| `HOLD_NUM` | `rack_count` |
| `STA_LOC` (자치구) | `region_id` 매핑 |

---

### 2.5 자전거 교통사고 다발지역 정보

| 항목 | 내용 |
|---|---|
| 제공기관 | 도로교통공단 |
| 식별번호 | `15056681` |
| 엔드포인트 | `http://apis.data.go.kr/B552061/frequentzoneBicycle/getRestFrequentzoneBicycle` |
| 인증키 | `TAAS_SERVICE_KEY` (공공데이터포털) |
| 이용허락 | **이용허락범위 제한 없음** |

**선정조건**: 반경 200m 내 자전거 사고 4건 이상 (사망사고 포함 시 3건)

**적재 결과 — 2024년 서울 111건** (2026-08-13)

| 지표 | 값 | 해석 |
|---|---|---|
| 수집 / UPSERT | 111 / 111 | 좌표 결손·지역 미상 0건 |
| 데이터 있는 자치구 | **19 / 25** | 6개 구는 `03 NODATA`. 사고다발지가 없다 |
| 폴리곤 결손 | 0 | 전 건에 `geom_json`이 있다 |
| 지오메트리 타입 | `ST_MultiPolygon` 111 | V3에서 통일했다 |
| 등급 분포 | CAUTION 96 / WARNING 9 / DANGER 6 | 대부분 4~5건짜리다 |
| MVP 경계 상자 내 | 73 | ⚠️ 아래 주의 |

> ⚠️ **`mvpAreaCount` 73건은 "한강 자전거도로 위"가 아니다.** `ridely.mvp-area`는
> 위도 37.500~37.610 / 경도 126.780~127.130의 **사각형**이고, 이 안에 서울 도심 대부분이 든다.
> 실제로 코스에 영향을 주는 건수는 노선 형상에서 반경을 잡아 다시 세야 한다.
> 이 값은 적재 검증용 지표이지 회피 대상 건수가 아니다.

**호출 방식 (확정)**

- **`guGun`은 필수다.** 서울 25개 자치구를 각각 호출한다 (`SeoulDistrict` enum, 활용가이드 3.3)
- `searchYearCd`는 동작한다. 4자리 연도(`2024`)를 넣는다
- 응답 상한이 16,000 byte, `geom_json` 하나가 최대 4,000 byte라 `numOfRows`를 **10**으로 잡았다
- 호출 수는 25 × 페이지. 데이터가 희소해 대개 구당 1페이지로 끝난다 (개발계정 일 1,000건 한도)

**응답 특성**

- `geom_json`이 **GeoJSON 문자열**로 온다. `ST_GeomFromGeoJSON()`에 바로 넣을 수 있다
- 폴리곤은 33개 점의 반경 200m 원형 근사다
- 응답 구조가 TourAPI와 다르다. `response.header/body` 래핑이 없고 정상 코드가 `"00"`이다
- `sido_sgg_nm`에 일련번호가 붙고 **표기가 연도별로 흔들린다** (2023 `"서울 강남구1"` vs 2022 `"서울특별시 강남구1"`).
  지역 매핑은 이름이 아니라 `bjd_cd` 앞 2자리로 한다
- **데이터가 희소하다.** 서울 전역 111건, 6개 구는 아예 없다. 자전거 사고다발지는 도심 교차로에 생기고
  한강 자전거도로에는 거의 없다 — 회피 기능의 실효는 제한적이다

명세와 실제가 어긋난 3건은 **6.6**에 정리했다.

---

### 2.6 자전거보관소정보 조회서비스

| 항목 | 내용 |
|---|---|
| 제공기관 | 행정안전부 (지역디지털협력과) |
| 식별번호 | `15155044` |
| 엔드포인트 | `https://apis.data.go.kr/1741000/bicycle_parking_info/info` |
| 인증키 | 공공데이터포털 키 |
| 갱신 | **매일** (2일 전 기준 현행화) |
| 이용허락 | **이용허락범위 제한 없음** |

**검증 결과 — 서울 2,698건**

- 좌표가 `WGS84_LAT`/`WGS84_LOT`로 온다
- `MNG_NO`가 고유 관리번호다 → `bike_parking.mgmt_no` UPSERT 키
- `cond[LCTN_ROAD_NM_ADDR::LIKE]`로 시도 단위 조회가 가능하다
- `numOfRows` 최대 100 → 서울만 27회 호출

> ⚠️ 명세 설명에 **"좌표계 : 보정계수 안들어간 Bessel 중부원점TM(EPSG:5174)"**라고 적혀 있으나 실제 값은 `37.594052 / 126.998777`로 **WGS84가 맞다.** `ST_Transform` 없이 적재한다. 개선요청 대상이다 — 6.2 참조.

- 자치구별 결측 편차가 크다. `INSTL_SHP`·`INSTL_YR`·`SNSHD_INSTL_YN`이 비어 있는 자치구가 있다. 실제로 쓰는 이름·주소·좌표·보관대수·관리기관은 채워져 있다

---

### 2.7 서울시 공공자전거 실시간 대여정보

| 항목 | 내용 |
|---|---|
| 서비스명 | `bikeList` (루트 키는 **`rentBikeStatus`**) |
| 인증키 | `SEOUL_OPEN_API_KEY` |
| 이용허락 | **공공누리 제1유형** |
| 적재 | **하지 않는다** |

잔여 대수(`parkingBikeTotCnt`)는 변동이 잦아 DB에 넣지 않는다. 필요 시 실시간 호출한다.

---

### 2.8 전국자전거도로표준데이터 *(차후 반영)*

| 항목 | 내용 |
|---|---|
| 제공기관 | 행정안전부 (원 생산자: 지방자치단체) |
| 식별번호 | `15100063` |
| 엔드포인트 | `https://api.data.go.kr/openapi/tn_pubr_public_bike_road_api` |
| 인증키 | 공공데이터포털 키 |
| 이용허락 | ⚠️ **메타데이터에 표기 없음 — 착수 전 확인 필요** |

응답 22개 항목 중 20개가 `bike_road` 컬럼과 1:1 대응한다.

**미루는 이유**: 노선 형상(LineString)을 제공하지 않아 라우팅에 기여하지 않는다. `preferBikeOnlyRoads`는 ORS가 처리한다. 자세한 내용은 `SCHEMA_CHANGE_POI.md` 9.1 참조.

---

### 2.9 자전거대여수리점안내 *(보류)*

| 항목 | 내용 |
|---|---|
| 제공기관 | 한국수자원공사 |
| 식별번호 | `15155614` |
| 엔드포인트 | `https://api.odcloud.kr/api/15155614/v1/uddi:a5097ec8-a17a-4155-a4c1-f0e1a3488a42` |
| 이용허락 | **이용허락범위 제한 없음** |

CSV 205행 / 고유 96지점. **서울 0건**이고 경기 24건도 양평·여주·가평이라 MVP 구간 밖이다. 좌표가 없어 지오코딩이 필요하다. 국토종주 코스로 영역을 넓힐 때 재검토한다.

---

### 2.10 공간정보 오픈플랫폼 WMS/WFS *(미채택)*

| 항목 | 내용 |
|---|---|
| 제공기관 | 국토교통부 (V-World) |
| 엔드포인트 | `https://api.vworld.kr/req/wfs` |
| 레이어 | `lt_p_bycracks` (자전거보관소) |
| 인증키 | `V_WORLD_KEY` (도메인 등록 필요) |

2.6과 비교하기 위해 실호출했다. GeoJSON·WGS84로 응답하고 속성도 대응하지만 **고유 관리번호가 없어** UPSERT를 할 수 없다. 2.6을 채택했다.

### ⚠️ 이용약관 제약이 공공데이터포털과 다르다

V-World는 공공누리가 아니라 **자체 이용약관**을 적용한다. 제약이 훨씬 강하다.

> **제19조(저작권) ②** 회원은 서비스를 이용하여 얻는 정보를 **사전 승낙 없이 복사, 복제, 변경, 번역, 출판, 방송, 기타 등의 방법으로 사용하거나 이를 타인에게 제공할 수 없습니다.**

> **금지행위** — 운영기관의 동의 없이 **영리를 목적으로 서비스를 이용하는 행위**

우리가 하려던 건 WFS 응답을 **DB에 적재(복제)하고 가공해 서비스로 제공**하는 것이라 이 조항에 걸릴 소지가 있다. 반면 2.6(행안부)은 "이용허락범위 제한 없음"이다.

고유키 부재만으로도 미채택을 고려했고 **약관만 봐도 같은 결론이 나온다.** 재검토하게 되더라도 이 조항을 먼저 확인해야 한다.

**호출 시 주의** (재검토할 경우를 대비해 남긴다)

- `OUTPUT=application/json` → GeoJSON, `SRSNAME=EPSG:4326` → WGS84
- 기본 좌표계가 `EPSG:900913`(미터)이라 `SRSNAME` 명시가 필수다
- `EPSG:4326`일 때 BBOX 축 순서가 뒤집힌다 — `(ymin,xmin,ymax,xmax)`
- 서버 호출 시 `DOMAIN` 파라미터에 발급 시 등록한 도메인을 넣는다

---

## 3. 파일 데이터 배치 방법

### 방침

**기관 원본 파일은 공개 저장소에 커밋하지 않는다.**

자전거길 DB는 이용허락범위 제한이 없어 법적으로는 재배포가 가능할 것으로 보인다.
그럼에도 커밋하지 않는 이유는 **공개 저장소에 기관 데이터를 통째로 올리는 것이 적절하지 않다**는 판단이다. 우리 저장소는 서비스 코드를 두는 곳이지 공공데이터 미러가 아니다.

| 대상 | 공개 저장소 | 팀 내부 공유 |
|---|---|---|
| 기관 원본 ZIP·CSV | ❌ | 각자 `data.go.kr`에서 직접 |
| UTF-8 변환본 | ❌ | ✅ |
| 적재 코드·변환 스크립트 | ✅ | — |

### 배치 경로

`db/seed/`는 `.gitignore` 대상이다.

```
db/seed/
  ├─ 자전거길_노선좌표.csv
  └─ 자전거길_주변시설.csv
```

| 파일 | 출처 |
|---|---|
| 자전거길_노선좌표.csv | 자전거길 DB (`data.go.kr/data/3038533`) ZIP 내 `★국토종주 자전거길 노선좌표.csv` |
| 자전거길_주변시설.csv | 같은 ZIP 내 `★국토종주자전거길 주변시설 좌표정보.csv` |

**인코딩은 CP949 그대로 두면 된다.** 적재 코드가 변환한다.

파일이 없으면 적재가 실패한다. 오류 메시지에 이 경로가 안내된다.

---

## 4. 공공데이터가 아닌 외부 서비스

출처표시 대상은 아니지만 이용약관과 무료 한도가 별도로 적용된다.

| 서비스 | 용도 | 인증키 | 비고 |
|---|---|---|---|
| openrouteservice | 자전거 경로 라우팅 | `ORS_API_KEY` | OpenStreetMap 기반. 무료 한도 확인 필요 |
| Kakao Local | 주소·장소명 → 좌표 | `KAKAO_REST_API_KEY` | 백엔드 전용 |
| Kakao Map JS | 지도 렌더링 | `KAKAO_JS_API_KEY` | 프론트 전용 |
| Google Gemini | 코스 설계·코멘트 생성 | `GOOGLE_API_KEY` | `gemini-3.5-flash-lite` (2026-08-14 전환, 아래 참조) |

**ORS 검증 결과** (선유도공원 → 여의도한강공원)

- `distance` 4,822.9m — 직선거리 3.9km 대비 정상. **한강 자전거길을 제대로 탄다**
- `ascent` 137.9m인데 같은 응답의 고도 범위가 1~32m다. **SRTM 노이즈로 과대 계상된다.**
  `total_ascent_m`·`estimated_duration_min`에 영향이 간다.
  원인 분석과 보정 방법(크기 임계값이 아니라 경사 상한)은 `SCHEMA_CHANGE_POI.md` 6.5
- `elevation:true`면 bbox가 **6개 값(3D)**이다
- `instructions:false`를 넣어 불필요한 턴바이턴 데이터를 줄인다

### Gemini 모델 전환 (2026-08-14)

`gemini-2.5-flash`가 **예고된 은퇴일(10-16)보다 먼저 신규 사용자에게 차단됐다.**

```
404 This model models/gemini-2.5-flash is no longer available to new users.
```

기존 계획은 "결선 진출 시 `gemini-3.5-flash`로 이관"이었는데 **그 계획도 폐기한다.** 공식 문서가 3.5-flash를 이미 legacy로 분류했고, 우리는 11월 시상식까지 살아 있어야 한다.

처음엔 최신 stable인 `gemini-3.7-flash`로 옮겼는데 **무료 한도에 막혔다.** AI Studio 콘솔 실측이다.

| 모델 | RPM | RPD | 기본 thinking | 판정 |
|---|---|---|---|---|
| Gemini 3.7 Flash | 5 | 20 | on | ❌ |
| Gemini 3.6 Flash | 5 | 20 | medium | ❌ |
| **Gemini 3.5 Flash Lite** | **20** | **500** | **minimal** | ✅ 채택 |

**RPD 20이면 하루 10회 추천이 상한이다.** 파이프라인이 추천 1회에 LLM을 2번(설계·코멘트) 부르기 때문이다. 개발 중 테스트만으로 오전에 소진되고 게이트 리허설도 못 돌린다.

`RIDELY_SPRINT_MASTER` R4의 전제(`2.5-flash 10RPM/500RPD`)가 무너졌다. 3.x 상위 모델은 RPD가 25분의 1이다.

**Lite로 충분하다고 본 근거** — 우리 작업은 "목록에서 3곳 고르기 + 두 문장 쓰기"다. 공식 문서도 단순 조회·분류에는 minimal thinking을 권한다. 상위 모델의 추론력이 필요한 일이 아니다.

> 품질이 부족하면 설계만 Lite로 두고 코멘트를 상위 모델로 올린다. 다만 그러면 코멘트가 RPD 20에 묶인다. **근본 해결책은 유료 티어 전환이다** — 10월 심사에서 심사위원이 직접 눌러보면 500도 안심할 수 없고, Tier 1은 결제 연결만으로 즉시 적용된다.

**교훈 둘**

1. 은퇴 예고일을 안전 마진으로 삼으면 안 된다. 예고일 두 달 전에 신규 차단이 걸렸고, 공지가 아니라 404로 알게 됐다.
2. **모델 선택 기준이 성능이 아니라 무료 한도다.** 최신 stable을 고르는 것이 능사가 아니다. 공식 문서는 모델별 무료 한도 표를 더 이상 싣지 않으므로 AI Studio 콘솔에서 직접 확인해야 한다.

> W5 RAG용 `gemini-embedding-001`은 아직 유효하다. 다만 후속인 `gemini-embedding-2-preview`가 나와 있어 착수 시 다시 확인한다.

---

## 5. 이용허락 정리

| 데이터 | 제공기관 | 이용허락 |
|---|---|---|
| 국문 관광정보 서비스_GW | 한국관광공사 | 제한 없음 (사진은 별도 제약) |
| 자전거길 DB | 행정안전부 | 제한 없음 |
| 자전거 교통사고 다발지역 | 도로교통공단 | 제한 없음 |
| 자전거보관소정보 조회서비스 | 행정안전부 | 제한 없음 |
| 자전거대여수리점안내 | 한국수자원공사 | 제한 없음 |
| 서울시 자전거 편의시설 | 서울특별시 | 공공누리 제1유형 |
| 서울시 공공자전거 대여소 정보 | 서울특별시 | 공공누리 제1유형 |
| 서울시 공공자전거 실시간 대여정보 | 서울특별시 | 공공누리 제1유형 |
| **전국자전거도로표준데이터** | 행정안전부 | ⚠️ **표기 없음 — 개선요청 제출 예정, 6.3 참조** |
| 공간정보 오픈플랫폼 WMS/WFS | 국토교통부 | 자체 약관 (복제·변경 제한) — **미채택** |

### 남은 할 일

- [ ] 전국자전거도로표준데이터 이용허락 확인 — 개선요청 회신 대기 (6.3)
- [ ] **출처표시 위치 결정** — 공공누리 제1유형은 출처표시가 필수다.
      공모전 제출물과 앱 화면(설정 > 정보 등) 두 곳에 표기 위치를 정해야 한다

### 출처표시 문안 (초안)

```
본 서비스는 아래 공공데이터를 활용하였습니다.

한국관광공사 「국문 관광정보 서비스_GW」
행정안전부 「자전거길 DB」, 「자전거보관소정보 조회서비스」
도로교통공단 「자전거 교통사고 다발지역」
서울특별시 「자전거 편의시설」, 「공공자전거 대여소 정보」
```

---

## 6. 원본 데이터 결함 및 개선요청 현황

원본에서 확인한 결함과 그에 대한 우리 대응을 한곳에 모은다.
**개선요청을 넣은 항목은 회신에 따라 코드를 되돌릴 수 있으므로** "고쳐지면 무엇을 지울 수 있는지"까지 적어 둔다(6.10).

데이터셋 단위로 나눈다. 창구가 데이터셋별로 갈리기 때문이다 —
공공데이터포털과 서울 열린데이터광장은 별개다.

**표기**  ☑️ 회신 완료 · ✅ 제출 완료(회신 대기) · 🟡 초안 작성(미제출) · ⬜ 확인 필요

### 6.0 결함 요약

| 절 | 데이터 | 창구 | 결함 | 제출 |
|---|---|---|---|---|
| 6.1 | 행안부 자전거길 DB (3038533) | 공공데이터포털 | 9 | ✅ 3 / 🟡 5 / ⬜ 1 |
| 6.2 | 행안부 자전거보관소 (15155044) | 공공데이터포털 | 1 | 🟡 1 |
| 6.3 | 행안부 전국자전거도로표준데이터 (15100063) | 공공데이터포털 | 1 | 🟡 1 |
| 6.4 | 한국관광공사 국문 관광정보 (15101578) | 공공데이터포털 | 1 | ☑️ 1 |
| 6.5 | 한국관광공사 두루누비 | 공공데이터포털 | 3 | ☑️ 3 |
| 6.6 | 도로교통공단 사고다발지역 (15056681) | 공공데이터포털 | 3 | 🟡 3 |
| 6.7 | 서울시 자전거 편의시설 (`tvBicycleEtc`) | **서울 열린데이터광장** | 8 | 🟡 8 |
| 6.8 | 서울시 공공자전거 대여소 (`tbCycleStationInfo`) | **서울 열린데이터광장** | 1 | ⬜ 1 |
| | **합계** | | **27** | ☑️ 4 / ✅ 3 / 🟡 18 / ⬜ 2 |

**회신 4건은 모두 "우리 판단이 맞았다"로 끝났다.** 6.4는 삭제된 필드가 실제로 사용중지였고(쓰지 않기로 한 것이 맞았다), 6.5는 자전거길 서비스 자체가 종료됐다(미채택이 맞았다). 두 건 다 응답만 보고는 알 수 없어 문의로만 확인되는 종류였다.

**성격별로 나누면 셋이다.**

| 성격 | 해당 | 우회 가능성 |
|---|---|---|
| **데이터 값이 틀렸다** | 6.1의 2·3·5·8, 6.7의 3~8 | 로직으로 덮을 수 있다 |
| **문서가 틀렸다** | 6.2, 6.3, 6.4, 6.6 전부, 6.8 | 실호출로 알아낼 수밖에 없다 |
| **API가 요청과 다르게 동작한다** | **6.5** | 응답 값을 직접 집계해야 드러난다 |

> 6.3만 **우회가 불가능하다.** 이용허락을 모르는 채로 제출물에 넣을 수 없어 회신이 착수 조건이다.
> 나머지 25건은 전부 처리해 뒀고, 회신은 다음 이용자를 위한 것에 가깝다.

---

### 6.1 행정안전부 — 자전거길 DB (3038533) *(파일)*

| # | 결함 | 규모 | 우리 대응 | 제출 |
|---|---|---|---|---|
| 1 | **구간 구분 정보 없음.** 좌표 시퀀스만 있고 노선이 갈라지는 지점을 표시하지 않는다 | 13노선 중 5개 | 좌표 간격 **3km 초과 시 파트 분리** + MultiLineString | ✅ 1차 |
| 2 | **좌표 결손 2건.** 순번 38569·38570의 위도 자리에 문자열(`"5, latit"`). 따옴표 안 콤마로 열이 5개가 된다 | 2 / 53,416행 | 열 수 `!=4` 검사로 스킵 | ✅ 1차 |
| 3 | **라벨과 좌표 불일치.** `"낙동강길"`로 표기됐으나 좌표는 금강 유역(126.75, 36.0) | 3 | 없음 — FK를 좌표로 채운다 | ✅ 1차 |
| 4 | **두 CSV의 위경도 열 순서가 반대.** 노선좌표 3열=위도, 주변시설 3열=경도 | 전체 | 리더를 파일별로 분리하고 열 위치를 상수로 고정 | 🟡 2차 |
| 5 | **`이름` 컬럼에 시설명이 아니라 노선명.** 급수대는 한강 구간 75건이 전부 `한강종주길` | 다수 | 표시용으로 종류명(`급수대`)을 쓴다 | 🟡 2차 |
| 6 | **주변시설에 노선 코드 없음** | 전체 | `ST_DWithin` 500m 최근접 매칭 | 🟡 2차 |
| 7 | **노선명 표기가 코드북과 불일치** (`한강종주길` ↔ `한강종주자전거길`) | — | `ROUTE_META` 매핑 | 🟡 2차 |
| 8 | **완전 중복 109건.** 좌표가 소수점 7자리까지 동일하다 | 109 / 1,133행 | `(종류·이름·좌표)` 기준 제거 | 🟡 2차 |
| 9 | **근접 중복.** 좌표가 몇 미터씩 어긋난 같은 지점 등록. 8번과 달리 완전 일치가 아니라 적재 시 걸러지지 않는다 | 실측 표본에서 급수대 8건 중 3건 | 후보 수집 단계에서 30m 이내 접기 | ⬜ 미제출 |

> 9번 실측 (2026-08-14, 선유도~여의도 축) — `808↔807` 5.5m, `828↔829` 2.4m, `790↔791` 2.2m.
> 급수대 후보 8건 중 3건이 중복이라 **실제 지점은 5곳뿐인데 후보 예산 8개를 다 썼다.**
> TourAPI 쪽에도 있다. `상수동 카페거리`(108)와 `로렌스길`(109)은 좌표가 완전히 같다 — 이쪽은 행안부 소관이 아니다.
>
> 2차 개선요청 작성 시점 이후에 발견해 아직 넣지 않았다. 3차를 낼 일이 생기면 묶는다.
> 코드는 `InfraCandidateCollector.dedupeNearby`로 이미 대응했다.

> 4번에 딸린 함정 — 노선좌표 CSV 헤더가 `위도(LINE_XP)` / `경도(LINE_YP)`다.
> **X가 위도**라 통상 관례와 반대다. 2차 요청에 같이 담았다.

> ⚠️ **4~8번을 "요청에 넣지 않는다"고 적었던 판단은 뒤집혔다(2026-08-13).**
> 우회가 간단한 것은 맞지만, 묶어서 심각도 순으로 내면 경미한 항목이 중요한 항목을
> 희석하지 않는다. 오히려 배포본을 끝까지 써봤다는 신호가 된다.

---

### 6.2 행정안전부 — 자전거보관소정보 조회서비스 (15155044)

| # | 결함 | 실제 | 우리 대응 | 제출 |
|---|---|---|---|---|
| 1 | **명세의 좌표계 표기 오류.** 설명에 `보정계수 안들어간 Bessel 중부원점TM(EPSG:5174)`라고 적혀 있다 | `37.594052 / 126.998777` — **WGS84(EPSG:4326)** | `ST_Transform` 없이 그대로 적재 | 🟡 |

**같은 문서 안에서 두 정보가 모순된다.** 응답 컬럼명은 `WGS84_LAT`·`WGS84_LOT`인데
설명은 EPSG:5174다. 설명을 믿고 변환을 걸면 **함수가 오류 없이 계산해버려** 좌표가
조용히 망가진다. 지도에 찍어보기 전까지 드러나지 않는다.

**결함이 아니라 개선 요망으로 분류한 것** — 좌표계 건이 묻히지 않게 이번 요청에서 뺐다.

- `numOfRows` 상한 100. 서울만 받는 데 27회 호출이 필요하다
- 자치구별 결측 편차. `INSTL_SHP`·`INSTL_YR`·`SNSHD_INSTL_YN`이 빈 자치구가 있다.
  다만 실제로 쓰는 이름·주소·좌표·보관대수·관리기관은 전 건 채워져 있다

---

### 6.3 행정안전부 — 전국자전거도로표준데이터 (15100063)

| # | 결함 | 우리 대응 | 제출 |
|---|---|---|---|
| 1 | **이용허락범위 미표기.** 메타데이터의 해당 항목이 비어 있다 | **착수 보류** | 🟡 |

관련법령(자전거 이용 활성화에 관한 법률 제13조, 같은 법 시행규칙 별지 제3호서식
자전거도로대장)은 안내돼 있으나 **이용허락범위는 별개 항목이고 비어 있다.**
법정 대장이라는 사실이 상업적 이용 가부나 출처표시 의무를 결정하지 않는다.

같은 기관의 3038533·15155044에는 "제한 없음"이 명시돼 있는데 이 데이터만 없다.

> **26건 중 유일하게 우회가 불가능하다.** 이용허락을 모르면 공모전 제출물에 넣을 수 없다.
> 다만 노선 형상(LineString)이 없어 라우팅에 기여하지 않으므로 급하지는 않다.

> 부수 소득 — **별지 제3호서식이 사실상 이 데이터의 항목 명세다.**
> 착수 시 포털 메타데이터보다 이 서식으로 컬럼을 대조하는 편이 정확하다.

---

### 6.4 한국관광공사 — 국문 관광정보 서비스_GW (15101578)

| # | 결함 | 우리 대응 | 제출 |
|---|---|---|---|
| 1 | **삭제된 필드가 응답에 잔존.** `areacode`·`sigungucode`·`cat1~3`이 명세에서 빠졌는데 값과 함께 계속 내려온다 | 사용하지 않는다. 지역 매핑은 `lDongRegnCd` | ☑️ **회신 완료 (2026-08-14)** |

폐기 예정이라 명세에서 먼저 뺀 것인지, 계속 제공되는데 명세 갱신 시 누락된 것인지
알 수 없었다. **"고쳐달라"가 아니라 "어느 쪽인지 알려달라"** 는 성격이라 문의로 냈다.

#### 회신 (한국관광공사 대외개방운영팀, 2026-08-14)

> `areacode`, `sigungucode`, `cat1`, `cat2`, `cat3` 는 모두 **사용중지**되었습니다. 조치예정일시 26년중.

**폐기 예정 쪽이었다. 쓰지 않기로 한 판단이 맞았다.**

응답에 값이 남아 있어도 언제든 사라질 수 있는 필드였다. 명세만 보고 판단하기 어려웠던 항목이라, 그대로 썼다면 값이 사라지는 시점에 지역·카테고리 분류가 조용히 깨졌을 것이다.

코드는 손댈 것이 없다. `TourIngestDao`가 이 컬럼들을 애초에 채우지 않고, `TourIngestDaoTest`가 NULL로 남는지 검사한다. 스키마에 컬럼 자리는 남아 있으나 계속 비워 둔다 — 지우려면 마이그레이션이 하나 늘고 얻는 것이 없다.

#### 공지 225 — 입출력 항목 데이터 변경 안내

**시행 2026-01-12.** 대상은 다국어 관광정보 9종 + 무장애 여행 정보로 총 10개 서비스, 오퍼레이션은 지역기반·위치기반·키워드·행사·숙박·동기화 전부다.

**요청항목과 응답항목 양쪽이 미표출된다.** 우리는 응답만 보고 있었는데 요청 파라미터로도 못 쓴다.

| 미표출 | 대체 |
|---|---|
| `areaCode` | `lDongRegnCd` (법정동 시도코드) |
| `sigunguCode` | `lDongSignguCd` (법정동 시군구코드, `lDongRegnCd` 필수) |
| `cat1` | `lclsSystm1` (분류체계 대분류) |
| `cat2` | `lclsSystm2` (`lclsSystm1` 필수) |
| `cat3` | `lclsSystm3` (`lclsSystm1`·`lclsSystm2` 필수) |

**우리 영향은 없다.** 요청·응답 양쪽에서 겹치는 항목이 하나도 없다.

| | 미표출 대상 | Ridely가 쓰는 것 |
|---|---|---|
| 요청 (`TourApiClient`) | `areaCode` `sigunguCode` `cat1~3` | `mapX` `mapY` `radius` `contentTypeId` `arrange` `numOfRows` `pageNo` |
| 응답 (`TourApiListResponse.Item`) | 위와 동일 | `contentid` `contenttypeid` `title` `addr1/2` `zipcode` `tel` `mapx/y` `firstimage(2)` `modifiedtime` `lDongRegnCd` `dist` |

`contentTypeId`는 미표출 목록에 없다. 관광타입 12·14 필터는 그대로 쓴다.

> ⚠️ **시행일이 이미 7개월 지났는데 값이 계속 왔다.** 우리가 개선요청을 낼 때 관찰한 것이 그 근거다. 공고한 날짜가 지켜지지 않았으므로 **예고 없이 사라질 수 있다고 보는 편이 맞다.** 쓰지 않기로 한 판단의 근거가 오히려 강해졌다.
>
> 지금도 값이 오는지는 실호출로 다시 확인할 수 있다. 다만 확인 결과와 무관하게 방침은 바뀌지 않는다.

#### 공지 230 — 신분류체계 관광타입 연계 안내

`cat1~3`의 대체가 `lclsSystm1~3`임이 확인됐다. 응답에는 이미 오고 있다(파서 테스트 표본에 포함).

**다만 제약이 하나 있다.**

> OpenAPI 요청항목 호출로 분류체계와 관광타입을 **현재는 연계 할 수 없습니다.** 추후 개선 하도록 하겠습니다.

`lclsSystm1~3`과 `contenttypeid`를 **요청에서 함께 쓸 수 없다.** 연계 정의는 첨부 파일로만 제공된다.

지금은 무관하다 — 관광타입(12·14)만으로 거르고 분류체계는 쓰지 않는다. **나중에 "관광지 중에서 공원만" 같은 조합이 필요해지면 이 제약에 걸린다.** 그때는 관광타입으로 받아 응답의 `lclsSystm`으로 애플리케이션에서 거르는 수밖에 없다.

연계 정의서(260112_01)를 받아 분류 구성을 2.1에 정리했다. 우리가 적재하는 12·14가 소분류 122건에 해당하고, 그중 자전거 코스에 쓸 만한 중분류 5종을 추려 뒀다.

시군구 단위가 필요해지면 `lDongSignguCd`를 응답 DTO에 추가하면 된다. 값은 이미 내려온다.

---

### 6.5 한국관광공사 — 두루누비 정보 서비스 *(미채택)*

**26건 중 유일한 기능 오류다.** 데이터 값이나 문서가 아니라 API가 요청과 다르게 동작한다.

| # | 결함 | 실측 | 제출 |
|---|---|---|---|
| 1 | **`brdDiv` 필터가 적용되지 않는다.** `DNBW`(자전거)를 요청해도 `DNWW`(걷기)가 온다 | `courseList&brdDiv=DNBW` → 144건, 반환 항목 전부 `DNWW` | ☑️ **회신 완료 (2026-08-14)** |
| 2 | **`routeList`가 모든 조건에서 0건** | `DNWW` 0 / `DNBW` 0 | ☑️ 위와 함께 회신 |
| 3 | **활용매뉴얼 v4.1 예시가 현 서비스에 없다.** 예시는 `밀양강 자전거길`·`DNBW`·`totalCount 740` | 현재 144건, 자전거 0건 | ☑️ 위와 함께 회신 |

> **건수만 보면 "자전거 코스 144건 조회 성공"으로 읽힌다.**
> 응답 항목의 `brdDiv`를 직접 집계하고서야 0건임이 드러났다.
> 서버 필터를 신뢰했다면 걷기 코스를 자전거 코스로 오인한 채 만들었을 것이다.

기각 판정과 상세는 `SPIKE_DURUNUBI.md` 6장. 스파이크 코드는 제거했다(`b0a1152`).

#### 회신 (한국관광공사 대외개방운영팀, 2026-08-14)

> 두루누비 사이트에 **자전거길 서비스가 종료**되었다는 공지가 있어 우선 전달드립니다. 매뉴얼에 자전거길 관련 정보는 종료되어 사용하지 않는다고 문서 갱신하여 업로드하겠습니다. 조치예정일시 26년 8월중.

두루누비 공지 `notice_idx=10123` 제목이 **「두루누비 일반길 / 자전거길 서비스 종료 안내」**로 확인된다.

**미채택 판정이 맞았고, 자전거 데이터가 돌아올 계획은 없다.**

세 결함의 성격이 회신으로 정리됐다. 2번·3번은 서비스 종료의 결과다. **1번은 그와 별개로 남는 문제다** — 서비스가 끝났다면 `DNBW` 요청에 걷기 코스를 돌려줄 것이 아니라 빈 결과나 오류를 줘야 한다. 회신에 이 부분에 대한 답은 없었고, 매뉴얼 갱신으로 갈음하는 것으로 보인다.

> ~~개선되면 GPX `trkseg`로 6.1의 1번(3km 임계값)을 통째로 없앨 수 있는 유일한 소스다.~~
> **철회한다.** 서비스가 종료됐으므로 재검토 대상이 아니다. 3km 임계값은 계속 유지한다.

---

### 6.6 도로교통공단 — 자전거사고 다발지역 (15056681)

데이터가 아니라 **활용가이드(v1.1)가 실제와 다르다.** 세 건 모두 실호출로만 알 수 있었다.

| # | 결함 | 실제 | 우리 대응 | 제출 |
|---|---|---|---|---|
| 1 | **`geom_json` 타입 예시가 `Polygon`뿐.** 예시와 항목 설명 어디에도 다중 폴리곤 언급이 없다 | `MultiPolygon`으로도 온다 | V3에서 컬럼을 `MultiPolygon`으로 통일하고 `ST_Multi()`로 감싼다 | 🟡 |
| 2 | **JSON 응답 구조 미문서화.** 명세는 XML 예시만 싣는다 | `response.header/body` 래핑이 없고 정상 코드가 `"0000"`이 아닌 `"00"` | `TaasFrequentZoneResponse`를 실응답 기준으로 작성 | 🟡 |
| 3 | **에러 코드표에 `03`이 없다.** 표에는 1·10·12·20·22·30·31·32·99뿐 | 사고다발지 없는 자치구는 `03 NODATA_ERROR` | `RESULT_CODE_NO_DATA`로 정상 처리 | 🟡 |

> **3번은 실제로 막혔던 항목이다.** 표에 없으니 오류로 잡았고 첫 자치구(종로구)에서
> 적재 전체가 멈췄다. 공공데이터포털 공통 코드라 생략한 것으로 보이는데,
> **이 API는 데이터 없음이 예외가 아니라 25개 구 중 6개(24%)의 정상 경로다.**

세 건 다 문서가 XML 기준으로만 쓰인 탓이라 **"JSON 응답 명세 보강" 하나로 묶어** 낸다.

---

### 6.7 서울특별시 — 자전거 편의시설 (`tvBicycleEtc`)

⚠️ 창구가 **서울 열린데이터광장**이다. 공공데이터포털이 아니다.

앞의 둘은 데이터 구조 문제고, 나머지 여섯은 개별 값 문제다.

| # | 결함 | 규모 | 우리 대응 | 제출 |
|---|---|---|---|---|
| 1 | **시설 종류 전용 필드 없음.** `THMS_TYPE`·`THMS_NM`이 전 건 동일해 구분에 못 쓴다 | 3,374 | `SeoulFacilityClassifier` — 시설명·상세정보 조합으로 추정 | 🟡 |
| 2 | **`FCLT_ID`를 고유 식별자로 쓸 수 없다.** 자치구마다 형식이 다르다 — `광진구_자전거종합서비스센터` / `11103419_1740730605325` | 전체 | **`(이름, 좌표)` 자연키 신설 (V2 마이그레이션)** | 🟡 |
| 3 | **`NM10` 항목명 결손.** 값(`VL10`) 1,043건이 채워져 있는데 항목명이 없다. 값은 전부 관리부서명 | 1,043 | 사용하지 않음 | 🟡 |
| 4 | **`VL08`(이동수리센터 순회일정)에 관리부서명.** `강동구청 교통행정과` ×2, `노원구청 탄소중립도시과` ×1 | 3 | 판정에서 제외 | 🟡 |
| 5 | **`VL06`(운영형태, 수리센터 전용)에 비수리시설.** `수락산집하장`, `자전거거치대(이설 및 철거)` | 2 | **그대로 적재** — 6.9.2 | 🟡 |
| 6 | **`VL01`(운영시간)에 위치·시설 설명.** `신림역 5번출구에서 직진방향` 등 | 2+ | 가공 없이 저장 | 🟡 |
| 7 | **시설명 오타.** `자잔거`, `잦너거 공기주입기` | 2+ | `주입기` 한 단어로 흡수 | 🟡 |
| 8 | **동일 주소 중복 등록.** `도림천자전거수리센터` / `자전거수리센터` 모두 `관악구 남부순환로 1583` | 1쌍 | 이름이 달라 자연키로는 별건. 두 건으로 적재됨 | 🟡 |

**2번이 영향이 가장 크다.** 나머지는 값 몇 건이 잘못 들어간 수준인데 이건 스키마를 바꾸게 만들었다.

**3번이 4~6번의 원인으로 보인다.** 관리부서를 적을 칸이 사실상 있는데 이름표가 없으니
자치구가 아무 데나 쓴다. 요청에서 이 논지로 묶었다.

> 4번과 5번은 **다른 건이다.** 겉모습이 닮아 헷갈리기 쉽다.
>
> | | 4번 (VL08) | 5번 (VL06) |
> |---|---|---|
> | 대상 | `일반거치대` 2건 (VL08 부서명 3건 중 2건) | `수락산집하장`, `자전거거치대(이설 및 철거)` |
> | 처리 | **제외** — 판정에 08을 안 쓴다 | **적재** — 06은 쓴다 |
> | 결과 | 우리 데이터에 없음 | 오탐으로 남아 있음 |

**1번과 7번은 연결돼 있다.** 종류 필드가 없어 시설명 문자열에 의존하는데 그 시설명에 오타가 있다.
`공기주입`으로 잡으면 공백 변형을 놓쳐 1,098건이 된다 — `주입기` 한 단어라야 전부 잡힌다.

**부가로 낸 것** — https 미지원(`http` + 8088 고정). 인증키가 URL 경로에 들어가 평문 전송된다.

---

### 6.8 서울특별시 — 공공자전거 대여소 정보 (`tbCycleStationInfo`)

| # | 결함 | 실제 | 우리 대응 | 제출 |
|---|---|---|---|---|
| 1 | **JSON 루트 키가 서비스명과 다르다** | 서비스명 `tbCycleStationInfo` → 루트 키 `stationInfo` | 실응답 기준으로 모델 작성 | ⬜ |

실시간 대여정보(`bikeList`)도 루트 키가 `rentBikeStatus`다.
서비스명으로 찾으면 **전 건 파싱에 실패하는데 오류 코드는 나지 않아** 0건으로 오인하기 쉽다.

> ⬜ **제출 전 확인 필요.** 명세에 루트 키가 이미 적혀 있다면 내지 않는다.
> 우리가 서비스명으로 찾다 실패한 것뿐일 수 있다.

---

### 6.9 결함이 아닌 것

확인 결과 원본 문제가 아니었던 항목이다. 재논의 시 이 절을 근거로 삼는다.

#### 6.9.1 노선 미연결 15건 (1.5%)

`route_facility` 1,024건 중 반경 500m 안에 국토종주 노선이 없는 시설이다. **전부 사유가 확인됐다.**

| 분류 | 건수 | 최근접 노선·거리 | 원인 |
|---|---|---|---|
| 한강 반대편 강안 | 8 | 한강종주 712~1,259m | 한강 폭이 약 1km다. 우리 형상은 한쪽 강안만 담는다 |
| 안동댐 | 2 | 낙동강 2,095m | 공식 국토종주가 **"낙동강자전거길(안동댐 구간 제외)"**이라 노선 데이터에 없다 |
| 금강 유역 | 3 | 금강 949~2,642m | 6.1의 3번(라벨 오류) |
| 경계 | 2 | 남한강 794m / 낙동강 579m | 목행교·창녕. 아슬아슬하게 밖 |

**반경을 넓히지 않는다.** `/pois/nearby`는 좌표 반경 검색이라 FK를 쓰지 않으므로
미연결 시설도 지도에 정상 표시된다. 반대로 넓히면 남한강·북한강·한강종주가
팔당 부근에서 만나 엉뚱한 노선에 붙을 위험이 커진다.

#### 6.9.2 수리센터 오탐 2건 — 그대로 둔다

24건 중 2건이 수리센터가 아니다(6.7의 5번). 빼지 않은 이유는 이렇다.

- 규칙을 "제공자 입력을 따른다"로 정했다. 이름으로 뒤집으면 원칙이 무너진다
- 거치대 옆에 간이 수리대를 두는 사례가 있어, 이름으로 거르면 진짜 수리소를 놓친다
- 둘 다 **노원구라 MVP 구간(한강)과 무관**하다
- 24건 중 2건이고, 피해는 "수리소로 표시됐는데 집하장" 수준이다

**검수 지표가 작동했다.** `hasRepairScope: false` 8건만 보면 됐고 그중 2건이 문제였다.
`true` 16건은 오탐 0건이다. 화면에 실제로 노출될 때 다시 판단한다.

#### 6.9.3 TourAPI 콘텐츠 건수 변동

2026-08-01 적재 시 734건, 08-12 재적재 시 **710건**.
수집 로직은 동일(1,019건 수신·UPSERT)하므로 원본 콘텐츠가 바뀐 것으로 본다.
공모전 제출물에 건수를 기재할 때는 그 시점에 다시 센다.

---

### 6.10 개선요청 회신 시 되돌릴 것

| 고쳐지면 | 지울 수 있는 것 |
|---|---|
| 6.1-1 구간 구분 컬럼 추가 (또는 GeoJSON/SHP/GPX 제공) | `BikeRouteIngestServiceImpl`의 **3km 임계값과 `splitByGap()`**. `SCHEMA_CHANGE_ROUTE_GEOM.md` 4장 근거 전체 |
| 6.1-2 좌표 결손 수정 | `BikeRouteCsvReader`의 스킵 허용치를 0으로 조일 수 있다 |
| 6.1-6 주변시설에 노선 코드 추가 | `RouteFacilityDao.linkToNearestRoute`의 좌표 추정 매칭 → 코드 직접 매핑 |
| ~~6.5 두루누비 자전거 코스 복구~~ | **철회.** 자전거길 서비스가 종료됐다(2026-08-14 회신). 3km 임계값을 없앨 유일한 대안이었으나 더는 기대할 수 없다 |
| 6.7-1 시설 종류 필드 추가 | `SeoulFacilityClassifier`의 문자열 매칭 규칙 + 단위 테스트 8개 |
| 6.7-2 공통 규칙 고유 ID 부여 | `repair_shop`의 `(이름, 좌표)` 자연키. 다만 V2로 이미 들어가 있어 되돌리는 비용이 더 클 수 있다 |

**되돌릴 것이 없는 항목** — 6.1의 3·4·5·7·8, 6.2, 6.4, 6.5, 6.6 전부, 6.8.
코드가 이미 실제에 맞춰져 있어 문서가 고쳐져도 그대로다. 다음 이용자를 위한 제보에 가깝다.

> **회신 온 두 건(6.4·6.5)이 이 표에서 빠졌다.** 6.4는 사용중지 확정이라 되돌릴 것이 애초에 없었고, 6.5는 서비스 종료로 기대 자체가 사라졌다. **3km 임계값은 영구적으로 유지한다** — 대안 소스 넷을 이미 확인했고(1장) 마지막 하나였던 두루누비가 닫혔다.

**6.3은 반대다.** 회신이 와야 착수한다.

> ⚠️ 회신은 수 주 걸릴 수 있고 반영 안 될 가능성도 크다.
> **현재 구현은 회신과 무관하게 완결돼 있다.** 위 표는 "나중에 정리할 수 있는 부채"이지
> 대기 중인 작업이 아니다.
