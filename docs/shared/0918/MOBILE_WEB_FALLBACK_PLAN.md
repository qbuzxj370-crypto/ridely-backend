# 모바일 웹페이지 버전 작업 계획 (2026-09-18)

**상태: 코드 작업 완료.** 남은 건 배포 인프라(호스팅·HTTPS·카카오맵 도메인) 쪽뿐이고, 전부 이 팀 관할 밖(EC2/카카오 콘솔 담당자)으로 확인됨 — 아래 "열린 질문" 참고.

## 배경 / 목적

스토어(구글 플레이·원스토어) 등록에 차질이 생겼을 때를 대비해, 지금 Cordova로 패키징해 APK로 배포하는 프론트(`android-shell/www/`)를 **모바일 Chrome에서 URL로 바로 접속해 쓰는 웹페이지**로도 돌아가게 만든다. 브랜치: `feature/mobile-web-fallback`.

## 조사 결과 — 생각보다 적게 고치면 된다

`android-shell/www/`를 전수 조사한 결과, Cordova 전용 API(`cordova.exec`, 네이티브 플러그인 JS 등)에 의존하는 코드가 **거의 없다**. 이미 표준 웹 API 위주로 짜여 있다:

| 기능 | 사용 API | 웹에서 그대로 되는가 |
|---|---|---|
| GPS (`riding.js`) | `navigator.geolocation` (표준 API) | ✅ 됨. `cordova-plugin-geolocation`이 설치돼 있지만 이 플러그인은 네이티브 환경에서 표준 API를 그대로 구현해주는 것뿐이라, 플러그인이 없는 웹에서는 브라우저 내장 Geolocation API가 알아서 대신함 |
| 로컬 저장(`ride-storage.js`, `token-store.js` 등) | `localStorage` (표준 API) | ✅ 됨 |
| 지도 | 카카오맵 JS SDK (`<script>` 로드) | ✅ 됨 (단, 도메인 화이트리스트는 새로 등록 필요 — 아래 "열린 질문" 참고) |
| 안드로이드 하드웨어 뒤로가기 | `backbutton` 커스텀 이벤트 (Cordova 전용) | 이벤트 자체가 웹에선 안 뜸 — 근데 우리 라우터가 `location.hash` 기반이라 해시 변경마다 브라우저 히스토리에 쌓여서, 모바일 브라우저의 기본 뒤로가기가 **아무 코드 없이 이미 똑같이 동작**함 |

**딱 하나, 실제로 막는 게 있다**: `www/js/app.js`가 `deviceready` 이벤트를 무조건 기다리는데, 이 이벤트는 Cordova 네이티브 브릿지(`cordova.js`)가 있어야만 발생한다. 오늘 낮에 정적 서버로 직접 테스트해보다가 이것 때문에 화면이 완전히 빈 채로 멈추는 걸 이미 직접 확인했다 — `cordova.js`가 404여도 모듈 스크립트 실행 자체는 막히지 않지만, `deviceready`를 영영 못 받아서 라우터가 시작을 안 한다.

## 설계 방향 (검토 요청)

### 방향 A — 코드베이스 하나 공유 (추천)

`www/` 폴더 하나를 그대로 두고, Cordova 빌드와 웹 배포 양쪽에 다 쓴다. `app.js`만 아래처럼 분기:

```js
if (window.cordova) {
  document.addEventListener('deviceready', onDeviceReady, false);
} else {
  onDeviceReady(); // 웹: cordova.js가 없으니 곧바로 시작
}
```

`index.html`의 `<script src="cordova.js">`는 그대로 둔다 — 실제 Cordova 빌드 때는 빌드 시스템이 진짜 `cordova.js`를 그 자리에 생성해서 넣어주고(레포에는 없음, gitignore 대상), 웹으로 그냥 열면 이 한 줄이 404 나는 것뿐이라 콘솔 에러 하나 빼고는 무해하다. 즉 **코드 변경은 사실상 `app.js` 몇 줄이 전부**고, 유지보수할 코드베이스가 늘어나지 않는다.

### 방향 B — www 폴더를 통째로 복제해서 웹 전용 사본을 따로 관리

앱과 웹을 앞으로 다르게 발전시킬 계획이 있다면 이쪽이 맞을 수 있다. 대신 지금부터 모든 화면 변경을 두 곳에 중복 적용해야 해서 드리프트(둘이 슬금슬금 달라지는 것) 위험이 생긴다.

**→ A로 진행 (승인 완료, 2026-09-18).**

## 작업 항목

1. [x] `app.js`: `deviceready` 대기를 `window.cordova` 유무로 분기
2. [x] `www/js/index.js` 삭제 — `cordova create` 템플릿이 만든 죽은 파일, 어디서도 안 씀
3. [x] 로컬 정적 서버 + Chrome(모바일 뷰포트 390×844)으로 8개 화면 전부 렌더링 확인 (홈/인증/코스추천/저장경로/라이딩/라이딩이력/마이페이지), 해시 라우터 기반 브라우저 뒤로가기 동작 확인
4. [x] 버그 수정 — 테스트에서 발견된 문제 없음 (핵심 변경이 `app.js` 한 곳뿐이라 예상대로 사이드이펙트 없음)
5. [x] 로직 검토 — 아래 "테스트 결과" 참고
6. [x] PWA(홈 화면 설치) 추가 — `manifest.json` + `sw.js` + 아이콘(192/512)
7. [x] 최종 정리 + 문서 갱신 (이 문서, `FRONTEND_ISSUES.md` 13번 항목)

## 테스트 결과 (2026-09-18)

- `window.cordova`가 없는 환경에서 `deviceready`를 기다리지 않고 라우터가 즉시 시작됨을 확인
- 홈/코스추천/라이딩/라이딩이력/마이페이지/저장경로/인증 7개 화면 전부 정상 렌더링 (로그인 게이트가 있는 화면은 가짜 토큰으로 우회해서 확인)
- 카카오맵은 예상대로 로드 실패(현재 JS 키가 `https://localhost` 도메인에만 등록돼 있어 테스트 서버 origin에서는 거부됨) — 에러가 배너로만 표시되고 나머지 화면 기능은 정상 동작해 실패 처리 자체는 문제 없음. 실제 웹 배포 도메인이 정해지면 그 도메인을 카카오 콘솔에 추가 등록해야 함(열린 질문 2번, 미해결)
- 해시 변경 → 브라우저 히스토리 자동 반영 → 뒤로가기 시 이전 화면 복귀까지 코드 수정 없이 확인됨
- PWA: manifest 링크·서비스워커 등록·아이콘 로드 전부 정상, 기존 화면 동작에 회귀 없음 확인 (`localhost`는 secure context로 취급돼 서비스워커 등록이 됨)

## 열린 질문 — 전부 확인 완료 (2026-09-18)

1. ~~호스팅 위치~~ → **EC2 정적 서빙으로 확정. 이 팀 관할 밖(EC2 담당자).** 실제 배포 주소가 정해지면 그때 `api.js`의 `API_BASE`·`index.html`의 CSP를 맞추면 됨(기존에 이미 문서화된 "배포 시 바꿔야 하는 3곳" 패턴과 동일). **PWA가 실제로 뜨려면 그 정적 서빙이 HTTPS여야 함 — EC2 담당자에게 전달 필요.**
2. ~~카카오맵 JS 키 도메인 등록~~ → **이 팀 관할 밖(카카오 콘솔 담당자).** 지금 앱 origin은 `https://localhost`(Cordova 전용)로 등록돼 있음. 웹 배포 도메인이 정해지면 그 도메인도 카카오 개발자 콘솔에 추가 등록해야 지도가 뜸 — 안 하면 지도만 배너 에러로 실패하고 나머지 기능은 정상 동작(이미 확인됨).
3. ~~PWA 지원 여부~~ → **하기로 결정, 완료.** `manifest.json` + `sw.js`(캐싱 없는 패스스루, 설치 가능 조건 충족용) + 192/512 아이콘(기존 `res/icon.png` 리사이즈, 임시 아이콘 교체되면 재생성 필요) 추가. **주의**: 서비스워커는 HTTPS(또는 localhost)에서만 등록된다 — 1번 항목의 HTTPS 요구사항과 동일한 이유.
4. ~~iOS Safari 지원~~ → **범위 제외 확정.** Android Chrome 기준으로만 작업.

## 남는 리스크

- 카카오맵 JS SDK가 `unsafe-eval`을 요구하는 CSP는 웹에서도 그대로 필요 — 보안적으로 이상적이진 않지만 지도 기능을 쓰는 한 불가피(기존 앱 버전과 동일 제약).
- 실제 모바일 기기의 Chrome에서의 최종 확인은 로컬 에뮬레이션만으로는 100% 대체 못 함 — 실기기 테스트 필요.
- PWA 아이콘(`www/icons/icon-*.png`)은 지금 임시 아이콘(`res/icon.png`)을 리사이즈한 것 — 실제 GPT 생성 아이콘으로 교체되면 이 두 파일도 같이 재생성해야 함.
- EC2가 HTTPS로 안 열리면 서비스워커 등록 자체가 안 돼서 PWA 설치 기능이 조용히 빠짐(에러 없이 그냥 "홈 화면에 추가"가 안 뜸) — 배포 후 반드시 확인 필요.
